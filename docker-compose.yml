version: '3.8'
services:
  guardrail:
    build: .
    container_name: guardrail
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./:/app:delegated
    restart: unless-stopped
